Powerline9K theme is in the custom folder. The fonts should be installed manually (since there are only 4 of them). The two color profiles in the iTerm2_Colors folder should be installed in iTerm2. Plugins is kept as a backup of the stuff to run. The .zshrc is the config file for Oh My ZSH. 

Sweet.